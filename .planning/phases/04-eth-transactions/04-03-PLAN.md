---
phase: 04-eth-transactions
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/wallet/store.ts
  - src/features/ui/App.tsx
  - src/features/ui/components/BalanceDisplay.tsx
  - src/features/ui/components/ActionButtons.tsx
  - src/features/ui/components/Header.tsx
  - src/features/ui/screens/MainScreen.tsx
  - src/features/ui/screens/SendRecipientScreen.tsx
  - src/features/ui/screens/SendAmountScreen.tsx
  - src/features/ui/screens/SendConfirmScreen.tsx
  - src/features/ui/screens/SendResultScreen.tsx
autonomous: false
requirements:
  - TX-01
  - TX-02
  - TX-06

must_haves:
  truths:
    - "User enters recipient address with validation feedback (checksum, length, format)"
    - "User enters ETH amount with fiat equivalent updating in real time"
    - "'Max' button fills maximum sendable ETH after gas deduction"
    - "Confirmation screen shows From, To, Amount (ETH + fiat), Fee, Total in card layout"
    - "Confirm button shows spinner + 'Sending...' while tx in flight"
    - "Success result shows checkmark + 'Sent X ETH to 0x...' + explorer link"
    - "Failure result shows error message + 'Try Again' + 'Cancel' buttons"
    - "Balance on main screen shows real ETH balance with fiat equivalent (replaces placeholder)"
    - "Shimmer skeleton shows while balance is loading"
    - "Recent addresses appear below recipient input"
  artifacts:
    - path: "src/features/ui/screens/SendRecipientScreen.tsx"
      provides: "Recipient entry with address validation + recent addresses list"
      contains: "SendRecipientScreen"
    - path: "src/features/ui/screens/SendAmountScreen.tsx"
      provides: "Amount entry with ETH/fiat toggle, Max button"
      contains: "SendAmountScreen"
    - path: "src/features/ui/screens/SendConfirmScreen.tsx"
      provides: "Card-style confirmation with gas details, Confirm/Cancel"
      contains: "SendConfirmScreen"
    - path: "src/features/ui/screens/SendResultScreen.tsx"
      provides: "Success/failure result with explorer link"
      contains: "SendResultScreen"
    - path: "src/features/ui/components/BalanceDisplay.tsx"
      provides: "Real ETH balance + fiat, replaces BalancePlaceholder"
      contains: "BalanceDisplay"
    - path: "src/features/wallet/store.ts"
      provides: "New screen types + send flow state"
      contains: "send-recipient"
  key_links:
    - from: "src/features/ui/screens/SendConfirmScreen.tsx"
      to: "src/features/wallet/messages.ts"
      via: "sendWalletMessage({ type: 'wallet:sendTransaction' })"
      pattern: "wallet:sendTransaction"
    - from: "src/features/ui/components/BalanceDisplay.tsx"
      to: "src/features/wallet/messages.ts"
      via: "sendWalletMessage({ type: 'wallet:getBalance' })"
      pattern: "wallet:getBalance"
    - from: "src/features/ui/App.tsx"
      to: "screens record"
      via: "Screen type -> component mapping"
      pattern: "send-recipient.*SendRecipientScreen"
---

<objective>
Build the complete send flow UI (4 screens) and replace the balance placeholder with real data.

Purpose: Connects the transaction backend (04-01) to user-facing screens. After this plan, a user can see their real balance, initiate a send, enter recipient/amount, review confirmation, submit, and see the result.

Output: 4 send screens, BalanceDisplay component, updated store with send state, wired App.tsx routing.
</objective>

<execution_context>
@/Users/torus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-eth-transactions/04-CONTEXT.md
@.planning/phases/04-eth-transactions/04-01-SUMMARY.md

<interfaces>
<!-- From 04-01: message types available to popup -->

WalletMessage variants (popup sends these):
```typescript
| { type: 'wallet:getBalance'; accountIndex: number }
| { type: 'wallet:estimateGas'; to: string; value: string; accountIndex: number }
| { type: 'wallet:getFeeParams' }
| { type: 'wallet:getEthPrice' }
| { type: 'wallet:sendTransaction'; to: string; value: string; accountIndex: number }
```

WalletResponse variants (popup receives these):
```typescript
| { type: 'wallet:balance'; balanceWei: string; balanceEth: string }
| { type: 'wallet:gasEstimate'; gasLimit: string; maxFeePerGas: string; maxPriorityFeePerGas: string; estimatedFeeWei: string; estimatedFeeEth: string }
| { type: 'wallet:feeParams'; baseFee: string; priorityFee: string; maxFeePerGas: string }
| { type: 'wallet:ethPrice'; usd: number }
| { type: 'wallet:txResult'; success: boolean; txHash: string; explorerUrl: string; error?: string }
```

From tx/build.ts (importable in popup for validation/formatting only -- no crypto):
```typescript
export function validateAddress(input: string): { valid: boolean; error?: string };
export function formatEth(wei: bigint): string;
export function formatUsd(amount: number): string;
export function parseEthToWei(eth: string): bigint;
export function calculateMaxSend(balanceWei: bigint, gasLimit: bigint, maxFeePerGas: bigint): bigint;
```

From types.ts:
```typescript
export interface RecentAddress { address: string; timestamp: number; }
```

Existing patterns:
- Screens are pure React components, use useWalletStore for nav
- sendWalletMessage() for all background communication
- Tailwind + zinc palette, dark mode via dark: prefix
- Header component handles back navigation automatically based on currentScreen
- 360x600px popup dimensions
- Jazzicon from src/lib/jazzicon.tsx for address avatars
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update store, create BalanceDisplay, wire ActionButtons + MainScreen + App.tsx</name>
  <files>
    src/features/wallet/store.ts,
    src/features/ui/components/BalanceDisplay.tsx,
    src/features/ui/components/ActionButtons.tsx,
    src/features/ui/components/Header.tsx,
    src/features/ui/screens/MainScreen.tsx,
    src/features/ui/App.tsx
  </files>
  <action>
    1. Update `src/features/wallet/store.ts`:
       - Add new Screen types: `'send-recipient' | 'send-amount' | 'send-confirm' | 'send-result'`
       - Add send flow state to WalletStore interface:
         ```typescript
         // Send flow state (passed between send screens)
         sendTo: string;
         sendAmountWei: string; // hex string
         sendAmountEth: string; // display string
         sendResult: { success: boolean; txHash: string; explorerUrl: string; error?: string } | null;
         setSendTo: (to: string) => void;
         setSendAmount: (wei: string, eth: string) => void;
         setSendResult: (result: { success: boolean; txHash: string; explorerUrl: string; error?: string } | null) => void;
         clearSendState: () => void;
         ```
       - Initialize send fields: sendTo: '', sendAmountWei: '', sendAmountEth: '', sendResult: null
       - `clearSendState` resets all send fields and should be called when leaving the send flow

    2. Create `src/features/ui/components/BalanceDisplay.tsx`:
       - Replace BalancePlaceholder with real balance display
       - On mount and when activeAccountIndex changes: call `sendWalletMessage({ type: 'wallet:getBalance', accountIndex })` and `sendWalletMessage({ type: 'wallet:getEthPrice' })`
       - State: `balanceWei: string | null`, `balanceEth: string | null`, `ethPrice: number | null`, `loading: boolean`
       - While loading: show shimmer skeleton animation (tailwind animate-pulse on zinc-200/zinc-800 bg rounded blocks matching the text layout)
       - When loaded: show ETH balance in large text (3xl bold) and fiat equivalent below (sm text, zinc-400/500)
       - Format: use formatEth for ETH, formatUsd for fiat (balanceEth parsed to float * ethPrice)
       - Same visual layout as BalancePlaceholder (centered, px-4, pt-8, pb-2 gap-1)
       - Note: formatEth, formatUsd, parseEthToWei can be imported in popup -- they're pure math functions with no crypto. BUT validateAddress uses micro-eth-signer's addr module. If micro-eth-signer imports cause bundle issues in popup (it pulls in crypto), create a thin `src/features/wallet/tx/format.ts` with just the formatters, separate from build.ts. Use judgment based on what typechecks cleanly.

    3. Update `src/features/ui/screens/MainScreen.tsx`:
       - Replace `<BalancePlaceholder />` import with `<BalanceDisplay />`
       - Remove BalancePlaceholder import

    4. Update `src/features/ui/components/ActionButtons.tsx`:
       - Remove `disabled` from Send button
       - Add `onClick={() => push('send-recipient')}` to Send button (mirrors Receive button pattern)

    5. Update `src/features/ui/components/Header.tsx`:
       - Add send screens to `SUB_SCREENS`: `new Set(['receive', 'settings', 'about', 'send-recipient', 'send-amount', 'send-confirm', 'send-result'])`
       - Add send screen titles to `SCREEN_TITLES`: `'send-recipient': 'Send', 'send-amount': 'Amount', 'send-confirm': 'Confirm', 'send-result': 'Result'`
       - Without this, send screens fall through to the branding header (no back button)

    6. Update `src/features/ui/App.tsx`:
       - Import all 4 new send screens
       - Add to screens record:
         ```
         'send-recipient': SendRecipientScreen,
         'send-amount': SendAmountScreen,
         'send-confirm': SendConfirmScreen,
         'send-result': SendResultScreen,
         ```
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
    Store has send screen types and send flow state. BalanceDisplay shows real balance with shimmer loading. Send button is enabled and navigates to send-recipient screen. All 4 send screens registered in App.tsx router. Typecheck and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build all 4 send flow screens</name>
  <files>
    src/features/ui/screens/SendRecipientScreen.tsx,
    src/features/ui/screens/SendAmountScreen.tsx,
    src/features/ui/screens/SendConfirmScreen.tsx,
    src/features/ui/screens/SendResultScreen.tsx
  </files>
  <action>
    1. Create `SendRecipientScreen.tsx`:
       - Header: handled by existing Header component (back arrow auto-detected)
       - Title: "Send to" in large text
       - Paste input field: text input with 0x placeholder, full-width, styled zinc border
       - Validation: on blur or onChange, validate using `validateAddress()`. Show red error text below input if invalid (from types: "Address must start with 0x", "Address must be 42 characters", "Invalid address checksum")
       - Recent addresses list: load from `chrome.storage.local.get('recentAddresses')` on mount. Display below input as clickable list items. Each shows jazzicon (16px) + truncated address (0x1234...abcd). Clicking fills the input.
       - "Next" button at bottom: enabled only when address is valid. Calls `setSendTo(address)` then `push('send-amount')`
       - Keyboard: auto-focus input on mount (useEffect + ref.focus() pattern per biome a11y)

    2. Create `SendAmountScreen.tsx`:
       - Fetch balance and price on mount: `sendWalletMessage({ type: 'wallet:getBalance', accountIndex })` and `sendWalletMessage({ type: 'wallet:getEthPrice' })`
       - Primary input: large centered number input for ETH amount. Below it: fiat equivalent in smaller text, updates on every keystroke (amount * ethPrice)
       - Toggle button to swap primary display (ETH <-> USD). When USD is primary, parse USD input -> divide by ethPrice -> show ETH equivalent below
       - "Max" button: fetch gas estimate first (`sendWalletMessage({ type: 'wallet:estimateGas', to: sendTo, value: '0x0', accountIndex })`), then calculate max = balance - (gasLimit * maxFeePerGas) using calculateMaxSend. Fill the ETH input with that amount. Show "0" if balance insufficient for gas.
       - Validation: amount > 0, amount <= balance (after accounting for gas). Show error text if exceeds balance.
       - "Insufficient balance" warning if amount + estimated gas > balance
       - "Next" button: enabled when valid amount entered. Calls `setSendAmount(weiHex, ethDisplay)` then `push('send-confirm')`
       - Show available balance at top: "Balance: X.XXXX ETH"

    3. Create `SendConfirmScreen.tsx`:
       - Card-style layout (per context decision): rounded border card in center of screen
       - From section: your active account with jazzicon + truncated address
       - Arrow separator
       - To section: recipient with jazzicon + truncated address. Tap to expand full address.
       - Amount: "X.XXXX ETH" in bold + "~$Y.YY" in smaller text
       - Network fee: "Network fee: X.XXXXXX ETH (~$Y.YY)" as single summary line
       - Expandable details section (collapsed by default): gas limit, max fee per gas (gwei), max priority fee (gwei). Toggle with chevron.
       - Total: amount + fee in ETH + fiat
       - Fetch gas estimate on mount: `sendWalletMessage({ type: 'wallet:estimateGas', to: sendTo, value: sendAmountWei, accountIndex })`
       - Fetch price on mount if not already cached in component state
       - Two buttons at bottom: "Cancel" (`clearSendState()` then `reset('main')` — NOT pop, which would go to send-amount) and "Confirm"
       - On Confirm click:
         - Disable button, show spinner + "Sending..." text
         - `sendWalletMessage({ type: 'wallet:sendTransaction', to: sendTo, value: sendAmountWei, accountIndex })`
         - On response: `setSendResult(response)` then `push('send-result')` (NOT replace — push keeps send-confirm on stack so "Try Again" can pop back to it)
       - On error in sendWalletMessage: setSendResult with success=false and error message

    4. Create `SendResultScreen.tsx`:
       - Read sendResult from store
       - Success state:
         - Green checkmark icon (large, animated -- simple scale-in CSS)
         - "Sent X.XXXX ETH" in bold
         - "to 0x1234...abcd" below
         - "View on Explorer" button: opens explorerUrl in new tab (`chrome.tabs.create({ url: explorerUrl })` or `window.open()`)
         - "Done" button: `clearSendState()` then `reset('main')`
       - Failure state:
         - Red X icon (large)
         - "Transaction Failed" heading
         - Error message in smaller text (the specific error from response)
         - "Try Again" button: `pop()` — returns to send-confirm (which is on stack since we used push, not replace)
         - "Cancel" button: `clearSendState()` then `reset('main')`
       - No auto-navigation -- user must explicitly dismiss

    Styling notes for all screens:
    - Zinc color palette, dark mode variants with dark: prefix
    - 360x600px popup -- use flex-col with overflow-y-auto for scrollable content
    - Buttons: use existing shadcn Button component where appropriate, or styled button elements matching existing patterns
    - Input styling: rounded-lg border border-zinc-300 dark:border-zinc-700 bg-white dark:bg-zinc-900 px-3 py-2 text-sm
    - Truncate addresses: `${addr.slice(0, 6)}...${addr.slice(-4)}`
    - Pass ETH price through screens via component state or store to avoid re-fetching (per pitfall 4 from research)
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
    All 4 send screens render correctly. Recipient screen validates addresses and shows recent addresses. Amount screen shows balance, ETH/fiat toggle, Max button. Confirm screen shows card layout with gas details. Result screen shows success/failure with appropriate actions. Full build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of send flow and balance display</name>
  <files></files>
  <action>
    What was built: Complete ETH send flow -- real balance display on main screen, 4-screen send flow (recipient -> amount -> confirm -> result), gas estimation with 60k floor, CoinGecko price integration, realtime tx submission.

    How to verify:
    1. Load extension in Chrome (chrome://extensions -> Load unpacked -> dist/)
    2. Verify main screen shows real ETH balance (or 0 ETH with $0.00 for unfunded wallet) with shimmer skeleton on initial load
    3. Click Send button -- should navigate to recipient entry screen
    4. Enter an invalid address (e.g., "0x123") -- should show validation error
    5. Enter a valid Ethereum address -- Next button should enable
    6. Click Next -- should navigate to amount screen showing your balance
    7. Try entering an amount larger than balance -- should show insufficient balance warning
    8. Click Max button -- should fill maximum sendable amount
    9. Click Next -- should show confirmation card with From, To, Amount, Fee, Total
    10. Click expandable gas details -- should show gas limit, max fee, priority fee
    11. (Optional if wallet has testnet ETH) Click Confirm to submit a test transaction
    12. Verify network toggle (mainnet/testnet) affects which RPC endpoints are called

    Resume signal: Type "approved" or describe issues
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>User confirms send flow works visually in Chrome extension</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. `pnpm build` produces working extension bundle
4. Send button on main screen is enabled (not disabled/grayed)
5. BalancePlaceholder component is no longer imported anywhere
6. All 4 send screens are registered in App.tsx screens record
7. Send flow state (sendTo, sendAmountWei, etc.) exists in zustand store
8. Address validation uses micro-eth-signer's addr module
9. ETH price is fetched from CoinGecko and cached
</verification>

<success_criteria>
- Balance display replaces placeholder with real ETH balance + fiat from CoinGecko
- Shimmer skeleton shows during balance loading
- Three-step send flow: Recipient -> Amount -> Confirmation (per context decision)
- Address validation with checksum, length, format checks
- Recent addresses stored and displayed
- ETH/fiat toggle on amount screen
- Max button calculates correct amount after gas deduction
- Card-style confirmation with expandable gas details
- Spinner on Confirm button during submission
- Success/failure result screens with appropriate actions
- Explorer link opens correct megaETH block explorer
- Build, typecheck, and lint all pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-eth-transactions/04-03-SUMMARY.md`
</output>
