---
phase: 01-build-extension-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/features/ui/App.tsx
  - src/features/ui/components/Header.tsx
  - src/features/ui/components/BalancePlaceholder.tsx
  - src/features/ui/components/ActionButtons.tsx
  - src/features/ui/ThemeProvider.tsx
  - src/components/ui/button.tsx
  - components.json
  - tests/build.test.ts
  - tests/pins.test.ts
autonomous: false
requirements:
  - BUILD-04
  - BUILD-06

must_haves:
  truths:
    - "Extension installs in Chrome from dist/ and shows styled popup at 360x600"
    - "Popup shows placeholder skeleton: header bar, balance area, action buttons"
    - "Light/dark mode toggle switches theme"
    - "Two consecutive production builds produce byte-identical dist/ output"
    - "No ^ or ~ version specifiers exist in package.json (test enforced)"
    - "pnpm install --frozen-lockfile succeeds (lockfile integrity)"
  artifacts:
    - path: "src/features/ui/App.tsx"
      provides: "Main popup layout with theme toggle"
    - path: "src/features/ui/components/Header.tsx"
      provides: "Header bar with megawallet branding and theme toggle"
    - path: "src/features/ui/components/BalancePlaceholder.tsx"
      provides: "Placeholder balance display area"
    - path: "src/features/ui/components/ActionButtons.tsx"
      provides: "Placeholder action buttons (Send, Receive)"
    - path: "tests/build.test.ts"
      provides: "Deterministic build verification"
      contains: "sha256"
    - path: "tests/pins.test.ts"
      provides: "Version pin verification"
      contains: "not.*toMatch.*[\\^~]"
  key_links:
    - from: "src/features/ui/App.tsx"
      to: "src/features/ui/components/Header.tsx"
      via: "import and render"
      pattern: "import.*Header"
    - from: "tests/build.test.ts"
      to: "dist/"
      via: "hash comparison after two builds"
      pattern: "hashDir.*dist"
---

<objective>
Add styled placeholder popup UI and build verification tests to complete the extension scaffold.

Purpose: Deliver a visually complete placeholder popup (header, balance area, action buttons with light/dark theme) and automated tests proving deterministic builds and exact version pinning.
Output: Chrome-loadable extension with styled skeleton UI + passing test suite.
</objective>

<execution_context>
@/Users/torus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-build-extension-scaffold/01-CONTEXT.md
@.planning/phases/01-build-extension-scaffold/01-RESEARCH.md
@.planning/phases/01-build-extension-scaffold/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create styled placeholder popup with light/dark theme</name>
  <files>src/features/ui/App.tsx, src/features/ui/components/Header.tsx, src/features/ui/components/BalancePlaceholder.tsx, src/features/ui/components/ActionButtons.tsx, src/features/ui/ThemeProvider.tsx, src/components/ui/button.tsx, components.json</files>
  <action>
1. **shadcn/ui setup** -- Create `components.json` manually (do NOT run `npx shadcn@latest init` -- it assumes Vite/Next.js per research Pitfall 5). Configure:
   - style: "new-york"
   - tailwind: { config: "", css: "src/styles/globals.css", baseColor: "zinc" }
   - aliases: { components: "@/components", utils: "@/lib/utils" }
   - tsx: true

   Then run `npx shadcn@latest add button` to get the Button component. If CLI fails, manually copy the button component source from shadcn/ui docs into src/components/ui/button.tsx.

2. **`src/features/ui/ThemeProvider.tsx`** -- Simple theme context. Store theme in localStorage. Apply 'dark' class to document.documentElement. Default to system preference via matchMedia. Export useTheme hook.

3. **`src/features/ui/components/Header.tsx`** -- Header bar component:
   - Left: "megawallet" text in monospace/bold (the brand)
   - Right: Theme toggle button (sun/moon icon) using the Button component
   - Monochrome background with subtle border-bottom
   - Use Tailwind classes, dark: variants for dark mode

4. **`src/features/ui/components/BalancePlaceholder.tsx`** -- Balance display area:
   - Large placeholder text "0.00 ETH" in center
   - Smaller placeholder text "$0.00" below
   - Subtle muted styling indicating these are placeholders
   - Account address placeholder: "0x0000...0000" with copy icon area
   - All placeholder, no real data

5. **`src/features/ui/components/ActionButtons.tsx`** -- Action button row:
   - Two buttons: "Send" and "Receive" side by side
   - Use Button component from shadcn/ui
   - Purple/violet accent color for primary actions
   - Disabled state (no functionality in Phase 1)

6. **`src/features/ui/App.tsx`** -- Update to compose all components:
   - Wrap in ThemeProvider
   - Fixed dimensions: w-[360px] h-[600px] (or min-h)
   - Layout: Header at top, BalancePlaceholder in center, ActionButtons at bottom
   - Overflow hidden, flex column
   - Both light and dark mode must look good

7. Update `src/styles/globals.css` if needed -- ensure @theme has the purple/violet accent, monochrome palette works for both modes. Add any dark mode CSS custom properties needed.

After creating all UI files, run `pnpm build` to verify the popup builds. Run `pnpm typecheck` and `pnpm lint` -- fix issues.
  </action>
  <verify>
    <automated>cd /Users/torus/Code/wallet && pnpm build && pnpm typecheck && pnpm lint && echo "UI PASS" || echo "UI FAIL"</automated>
  </verify>
  <done>Popup renders styled skeleton with header (megawallet + theme toggle), balance placeholder, and action buttons. Builds without errors. Light and dark themes both work via ThemeProvider.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic build and version pin verification tests</name>
  <files>tests/build.test.ts, tests/pins.test.ts, vitest.config.ts</files>
  <behavior>
    - Test: Two consecutive production builds produce identical SHA-256 hash of dist/ directory
    - Test: package.json dependencies and devDependencies have zero ^ or ~ version specifiers
    - Test: pnpm install --frozen-lockfile succeeds (lockfile matches package.json)
  </behavior>
  <action>
1. **`vitest.config.ts`** -- Create Vitest config. Set test.include to ['tests/**/*.test.ts']. Set test.timeout to 60000 (build tests are slow). Resolve alias @ -> ./src.

2. **`tests/build.test.ts`** -- Per research code example. Deterministic build test:
   - Run `pnpm build:prod` (production build), hash dist/ recursively via SHA-256
   - Run `pnpm build:prod` again, hash dist/ again
   - Assert hashes are identical
   - Hash function must recursively read all files in sorted order (deterministic traversal)
   - Use execSync for builds, createHash/readFileSync for hashing
   - Set timeout to 60s (two builds take time)

3. **`tests/pins.test.ts`** -- Per research code example. Version pin test:
   - Read package.json
   - For every entry in dependencies and devDependencies
   - Assert version string does not start with ^ or ~
   - Report which package fails if any do

After creating tests, run `pnpm test` and verify all pass.
  </action>
  <verify>
    <automated>cd /Users/torus/Code/wallet && pnpm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>vitest runs 2 test files. Deterministic build test passes (identical hashes). Version pin test passes (no ^ or ~). All tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify extension loads in Chrome with styled popup</name>
  <files>none (manual verification)</files>
  <action>
User manually loads the extension in Chrome and verifies the popup UI.
  </action>
  <what-built>Complete MV3 Chrome extension scaffold with styled popup UI, build pipeline, and verification tests</what-built>
  <how-to-verify>
    1. Open Chrome, go to chrome://extensions, enable Developer Mode
    2. Click "Load unpacked", select the `dist/` folder in the wallet project
    3. Extension should install without errors -- check for the megawallet icon in toolbar
    4. Click the megawallet icon -- popup should open at ~360x600 showing:
       - Header with "megawallet" text and theme toggle (sun/moon)
       - Balance area with "0.00 ETH" placeholder
       - Send and Receive buttons (disabled)
    5. Click theme toggle -- should switch between light and dark mode
    6. Open DevTools (right-click popup -> Inspect) -> Console: no CSP violations, no errors
    7. Check service worker: chrome://extensions -> megawallet -> "Service worker" link -> Console should show startup log
  </how-to-verify>
  <verify>
    <automated>cd /Users/torus/Code/wallet && pnpm build && pnpm test && pnpm typecheck && pnpm lint && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>Extension loads in Chrome, popup displays styled skeleton, theme toggle works, no CSP violations, service worker runs, all automated tests pass.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm test` passes (deterministic build + version pins)
2. `pnpm build` produces complete dist/ directory
3. Extension loads in Chrome without errors
4. Popup displays styled placeholder UI with working theme toggle
5. No CSP violations in DevTools console
6. Service worker starts without errors
7. `pnpm typecheck` passes
8. `pnpm lint` passes
</verification>

<success_criteria>
- Extension installs and runs in Chrome showing styled placeholder popup
- Light/dark theme toggle works
- Deterministic build test proves byte-identical output
- Version pin test enforces exact pinning
- Zero CSP violations or console errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-extension-scaffold/01-02-SUMMARY.md`
</output>
