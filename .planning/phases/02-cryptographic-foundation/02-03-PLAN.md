---
phase: 02-cryptographic-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/entrypoints/background.ts
  - src/features/wallet/crypto/index.ts
  - src/features/wallet/messages.ts
  - tests/crypto/isolation.test.ts
  - tests/crypto/integration.test.ts
autonomous: true
requirements:
  - SEC-02
  - SEC-03
  - SEC-06
  - SEC-07
  - SEC-10

must_haves:
  truths:
    - "background.ts registers message listeners for all WalletMessage types"
    - "wallet:create returns mnemonic + address but does NOT persist vault to storage.local yet (held in chrome.storage.session as pendingCreation — survives worker suspension)"
    - "wallet:confirmSeedPhrase validates word-index proof, then persists vault to storage.local and caches session; wallet is not usable until this is called (SEC-03)"
    - "wallet:import validates mnemonic, encrypts vault, returns address (no mnemonic in response)"
    - "wallet:unlock decrypts vault, caches seed in chrome.storage.session, returns address"
    - "wallet:lock clears chrome.storage.session"
    - "wallet:deriveAccount derives new account and returns DerivedAccount (no private key in response)"
    - "wallet:getAccounts returns array of DerivedAccount (address + index only, no keys)"
    - "Encrypted vault persisted in chrome.storage.local"
    - "Decrypted seed cached in chrome.storage.session (hex-encoded, not raw Uint8Array)"
    - "grep of entire src/ finds zero paths where private key bytes appear in WalletResponse"
    - "grep of entire src/ finds zero console.log/warn/error calls containing key/mnemonic/seed/private"
    - "Crypto module files only imported in background.ts, never in popup/content/inpage"
  artifacts:
    - path: "src/entrypoints/background.ts"
      provides: "Service worker with wallet message handlers"
      contains: "chrome.runtime.onMessage"
    - path: "src/features/wallet/crypto/index.ts"
      provides: "Barrel export for crypto module"
      contains: "export"
    - path: "src/features/wallet/messages.ts"
      provides: "Type-safe sendMessage wrapper for popup"
      contains: "sendWalletMessage"
    - path: "tests/crypto/integration.test.ts"
      provides: "Full create/unlock/derive/lock lifecycle test"
      contains: "lifecycle"
    - path: "tests/crypto/isolation.test.ts"
      provides: "Key isolation grep verification"
      contains: "isolation"
  key_links:
    - from: "src/entrypoints/background.ts"
      to: "src/features/wallet/crypto/index.ts"
      via: "imports crypto functions for message handling"
      pattern: "from.*wallet/crypto"
    - from: "src/features/wallet/messages.ts"
      to: "src/features/wallet/types.ts"
      via: "WalletMessage/WalletResponse types for sendMessage"
      pattern: "WalletMessage"
---

<objective>
Wire crypto modules into background service worker with Chrome message passing. Implement key isolation: encrypted vault in chrome.storage.local, decrypted seed in chrome.storage.session only while unlocked, private keys never in responses. Implement SEC-03 confirmation gate: vault not persisted until seed phrase confirmed. Verify isolation with grep-based audit.

Purpose: The integration layer that makes crypto usable while enforcing security boundaries. Popup sends messages, background handles them, keys never cross the boundary.
Output: Complete message-passing wallet engine. Lifecycle: create -> confirmSeedPhrase -> lock -> unlock -> derive -> getAccounts. Security grep audit passes.
</objective>

<execution_context>
@/Users/torus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-cryptographic-foundation/02-CONTEXT.md
@.planning/phases/02-cryptographic-foundation/02-RESEARCH.md
@src/features/wallet/types.ts
@src/features/wallet/crypto/mnemonic.ts
@src/features/wallet/crypto/hd.ts
@src/features/wallet/crypto/address.ts
@src/features/wallet/crypto/vault.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create barrel export, message helper, and background message handlers</name>
  <files>src/features/wallet/crypto/index.ts, src/features/wallet/messages.ts, src/entrypoints/background.ts</files>
  <action>
1. **`src/features/wallet/crypto/index.ts`** -- Barrel export:
   - Re-export all public functions from mnemonic.ts, hd.ts, address.ts, vault.ts
   - This is the single import point for background.ts

2. **`src/features/wallet/messages.ts`** -- Type-safe message wrapper for popup side:
   - `sendWalletMessage(msg: WalletMessage): Promise<WalletResponse>` -- wraps `chrome.runtime.sendMessage` with proper typing
   - This file does NOT import any crypto modules. It only imports types.
   - **CRITICAL:** This file is safe to import in popup because it contains zero crypto code, only message type definitions and a sendMessage wrapper.

3. **`src/entrypoints/background.ts`** -- Replace placeholder with full wallet message handler:

   **Imports:** Import crypto functions from `@/features/wallet/crypto`. Import types from `@/features/wallet/types`.

   **Storage helpers:**
   - `saveVault(blob: VaultBlob)` -> `chrome.storage.local.set({ vault: blob })`
   - `loadVault()` -> `chrome.storage.local.get('vault')` returns VaultBlob | null
   - `cacheSession(data: { seed: string; accounts: DerivedAccount[] })` -> `chrome.storage.session.set({ walletSession: data })`
     - **Hex-encode seed before storing** (chrome.storage serializes to JSON; Uint8Array becomes broken object)
   - `getSession()` -> `chrome.storage.session.get('walletSession')` returns session data or null
   - `clearSession()` -> `chrome.storage.session.remove('walletSession')`

   **Message handler:** `handleWalletMessage(msg: WalletMessage): Promise<WalletResponse>`

   **`wallet:create`:** (SEC-03: vault NOT persisted until seed confirmed)
   1. Generate mnemonic (using msg.strength or default 128)
   2. Convert mnemonic to seed
   3. Derive account at index 0
   4. Create VaultPlaintext `{ mnemonic, createdAt: Date.now() }`
   5. Encrypt vault with msg.password
   6. **Save pending state to `chrome.storage.session` under key `pendingCreation`** (NOT to storage.local). This includes the encrypted vault blob, hex-encoded seed, and derived accounts. Survives MV3 worker suspension but clears on browser close.
   7. Return `{ type: 'wallet:created', address, mnemonic }` -- this is the ONLY response that includes mnemonic
   - **CRITICAL:** Wallet is NOT usable at this point. No vault in storage.local, no session cached under `walletSession`. User must confirm seed phrase first.

   **`wallet:confirmSeedPhrase`:** (SEC-03: finalize creation after seed confirmed with proof)
   1. Read `pendingCreation` from `chrome.storage.session`. If absent, return error "No pending wallet creation".
   2. Validate `msg.wordIndices`: caller must provide the ordered indices of e.g. 4 randomly-chosen word positions; background checks each index maps to the correct word in the stored mnemonic. If any mismatch, return error "Seed phrase confirmation failed" (don't reveal which word was wrong).
   3. Save `pendingCreation.vault` to chrome.storage.local
   4. Cache seed (hex) + accounts in `chrome.storage.session` under `walletSession`
   5. Remove `pendingCreation` from `chrome.storage.session`
   6. Return `{ type: 'wallet:confirmed', address }`
   - **Durability:** Pending state lives in `chrome.storage.session`, so MV3 worker suspension/restart does NOT lose the user's creation flow. Session clears only on browser close — acceptable since user can regenerate.
   - `pendingCreation` is set by `wallet:create`, consumed by `wallet:confirmSeedPhrase`, removed from session on use or on `wallet:lock`

   **`wallet:import`:**
   1. Validate mnemonic with isValidMnemonic. If invalid, return error.
   2. Convert mnemonic to seed
   3. Derive account at index 0
   4. Create VaultPlaintext, encrypt, save to storage.local
   5. Cache seed + account 0 in storage.session
   6. Return `{ type: 'wallet:imported', address }` -- NO mnemonic in response

   **`wallet:unlock`:**
   1. Check lockout manager. If locked, return error with remaining time.
   2. Load vault from storage.local. If no vault, return error.
   3. Decrypt vault with password. If fails, record failure in lockout manager, persist lockout state to storage.session, return error.
   4. On success: reset lockout, convert mnemonic to seed, derive account 0, cache in storage.session.
   5. Return `{ type: 'wallet:unlocked', address }`

   **`wallet:lock`:**
   1. Clear storage.session
   2. Return `{ type: 'wallet:locked' }`

   **`wallet:getAccounts`:**
   1. Get session. If no session, return error "Wallet is locked".
   2. Return `{ type: 'wallet:accounts', accounts: session.accounts }`

   **`wallet:deriveAccount`:**
   1. Get session. If no session, return error "Wallet is locked".
   2. Decode seed from hex
   3. Derive account at requested index
   4. Add to session accounts list, update storage.session
   5. Return `{ type: 'wallet:derived', account: { index, address, path } }` -- NO private key

   **Listener registration (with sender authorization):**
   ```typescript
   chrome.runtime.onMessage.addListener((msg, sender, sendResponse) => {
     // Only accept messages from our own extension contexts
     if (sender.id !== chrome.runtime.id) return;
     // Reject content script / inpage origins — only popup and background are trusted
     if (sender.origin && !sender.origin.startsWith('chrome-extension://')) return;

     if (msg.type?.startsWith('wallet:')) {
       handleWalletMessage(msg as WalletMessage).then(sendResponse);
       return true; // async response
     }
   });
   ```
   - **Why:** Once content scripts and inpage providers are added (Phase 5), unvalidated senders could call privileged actions like wallet:create/import/unlock. Enforcing sender.id now prevents this class of bug from ever existing.

   **On service worker start:** Initialize lockout manager. Try to restore lockout state from storage.session.

   **SECURITY RULES (enforced in code):**
   - NEVER include `privateKey` in any WalletResponse
   - NEVER include `mnemonic` in any response except `wallet:created`
   - NEVER log mnemonic, seed, or private key values
   - ALL error messages are generic -- "Incorrect password", "Wallet is locked", "Invalid mnemonic"
  </action>
  <verify>
    <automated>cd /Users/torus/Code/wallet && pnpm typecheck && pnpm build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Background service worker handles all wallet messages. wallet:create holds vault in memory; wallet:confirmSeedPhrase persists to storage.local (SEC-03). Session cached in storage.session with hex-encoded seed. Private keys never in responses. Mnemonic only returned during create.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests and key isolation audit</name>
  <files>tests/crypto/integration.test.ts, tests/crypto/isolation.test.ts</files>
  <action>
1. **`tests/crypto/integration.test.ts`** -- Full lifecycle test (unit-level, mocking chrome APIs):

   **Setup:** Mock `chrome.storage.local` and `chrome.storage.session` as in-memory Maps. Mock `chrome.runtime.onMessage` and `chrome.runtime.sendMessage`. Import the handler functions directly for testing.

   Since background.ts is an entrypoint with side effects, extract `handleWalletMessage` as a testable exported function. The test imports it directly along with the storage helpers. Mock `chrome.storage.local` and `chrome.storage.session` as in-memory Maps — these mocks are **required**, not optional, because SEC-06/07/10 assertions depend on verifying actual storage interactions.

   **Test suite: "wallet lifecycle integration"**
   - **Create flow:** generateMnemonic -> mnemonicToSeed -> deriveAccount(seed, 0) -> encryptVault -> decryptVault -> verify mnemonic matches
   - **Create + confirm flow (SEC-03):** wallet:create returns mnemonic but vault not in storage.local (only in storage.session as pendingCreation) -> wallet:confirmSeedPhrase with correct wordIndices persists vault to storage.local -> wallet:getAccounts now works
   - **Create without confirm (SEC-03):** wallet:create then wallet:getAccounts -> error "Wallet is locked" (vault not in storage.local, walletSession not cached)
   - **Confirm with wrong words (SEC-03):** wallet:create then wallet:confirmSeedPhrase with incorrect word for a challenged position -> error "Seed phrase confirmation failed", vault still NOT in storage.local
   - **Pending survives mock worker restart (SEC-03):** wallet:create -> clear module state (simulate SW suspension) -> re-read pendingCreation from storage.session mock -> wallet:confirmSeedPhrase succeeds
   - **Import flow:** known mnemonic -> mnemonicToSeed -> deriveAccount -> verify address matches expected
   - **Multi-account:** derive indices 0-4, verify 5 unique addresses
   - **Lock/unlock cycle:** encrypt vault -> "lock" (discard seed) -> "unlock" (decrypt vault, recover seed) -> derive same account -> same address

   **Test suite: "message response safety"**
   - Define all WalletResponse variants as objects
   - Assert NONE of them contain a field named "privateKey"
   - Assert only `wallet:created` response has "mnemonic" field
   - This is a compile-time + runtime check: build the actual response objects and verify keys

2. **`tests/crypto/isolation.test.ts`** -- Grep-based security audit:

   **Test suite: "key isolation - static analysis"**
   Using Node.js `fs` + regex to scan source files:

   - **Test: "no private key in response types"**
     Read `src/features/wallet/types.ts`. Verify WalletResponse union type does NOT contain `privateKey` field. Parse the file as text and grep for `privateKey` within the WalletResponse type definition -- it should only appear in KeyPair (internal type), never in WalletResponse/DerivedAccount.

   - **Test: "crypto module not imported outside background"**
     Scan all .ts/.tsx files in `src/entrypoints/` and `src/features/`. The string `wallet/crypto` (import path for crypto module) should ONLY appear in:
     - `src/entrypoints/background.ts`
     - `src/features/wallet/crypto/` (internal imports within crypto module itself)
     NOT in popup.tsx, content.ts, inpage.ts, or any UI component.

   - **Test: "no key material in console output"**
     Scan all .ts/.tsx files in `src/`. No `console.log`, `console.warn`, `console.error` call should contain the substrings: `mnemonic`, `privateKey`, `seed`, `private` (case-insensitive check on the string argument). The background.ts startup log `[vibewallet]` is fine -- it logs a static string, not key material.

   - **Test: "messages.ts has zero crypto imports"**
     Read `src/features/wallet/messages.ts`. Verify it does NOT import from `@scure/`, `@noble/`, or `wallet/crypto`. It should only import types.

   These tests are the "grep of entire codebase finds zero paths" from the phase success criteria.

Run all crypto tests: `pnpm test -- tests/crypto/`. Everything must pass.
  </action>
  <verify>
    <automated>cd /Users/torus/Code/wallet && pnpm test -- tests/crypto/ && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Integration lifecycle tests pass (create, import, lock/unlock, multi-account). Key isolation audit passes: no private keys in responses, crypto only imported in background, no key material logged, messages.ts has zero crypto imports.</done>
</task>

</tasks>

<verification>
1. `pnpm test -- tests/crypto/` -- all suites pass (mnemonic, hd, address, vault, integration, isolation)
2. Full lifecycle: create -> confirmSeedPhrase -> lock -> unlock -> derive -> same addresses
3. SEC-03: wallet:create without confirmSeedPhrase leaves wallet unusable (no vault persisted)
4. Key isolation: grep-based tests confirm no key leakage paths
5. Crypto imports only in background.ts and crypto module internals
6. No console.log of key material anywhere in src/
7. messages.ts imports only types, zero crypto deps
8. `pnpm build` succeeds (background.ts compiles with crypto imports)
9. `pnpm typecheck` passes
10. `pnpm lint` passes
</verification>

<success_criteria>
- Background service worker handles create/confirmSeedPhrase/import/unlock/lock/derive/getAccounts
- wallet:create stores pending state in chrome.storage.session (survives worker suspension); wallet:confirmSeedPhrase validates word-index proof then persists vault (SEC-03)
- Wallet is not usable (getAccounts/derive fail) until seed phrase confirmed with correct words or wallet imported
- Encrypted vault persists in chrome.storage.local only after proof-of-confirmation or import
- Message listener validates sender.id === chrome.runtime.id before processing
- Decrypted seed cached in chrome.storage.session only while unlocked
- Private keys NEVER in any WalletResponse
- Mnemonic returned ONLY during wallet:create
- Grep-based audit confirms zero key leakage paths
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-cryptographic-foundation/02-03-SUMMARY.md`
</output>
